name: Daily Production Run

on:
  workflow_dispatch:
  schedule:
    - cron: "35 13 * * 1-5"
    - cron: "35 14 * * 1-5"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: daily-production-run
  cancel-in-progress: false

jobs:
  predict:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Check out repo
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Gate scheduled run to 09:30-09:45 America/New_York on weekdays
        id: gate
        if: github.event_name == 'schedule'
        run: |
          python - <<'PY'
          import csv
          import os
          import pathlib
          from datetime import datetime
          from zoneinfo import ZoneInfo

          now = datetime.now(ZoneInfo("America/New_York"))
          ok = (
              now.weekday() in {0, 1, 2, 3, 4}
              and now.hour == 9
              and 30 <= now.minute <= 45
          )

          root = pathlib.Path(os.environ.get("GITHUB_WORKSPACE", "."))
          log_path = root / "artifacts_part3" / "prediction_log.csv"
          today_str = now.date().isoformat()

          if ok and log_path.exists():
              try:
                  with open(log_path, newline="", encoding="utf-8") as f:
                      rows = list(csv.DictReader(f))
                  if rows:
                      last_decision = str(rows[-1].get("decision_date", "")).strip()[:10]
                      if last_decision == today_str:
                          print(f"Already ran for {today_str}, skipping duplicate scheduled run.")
                          ok = False
              except Exception as e:
                  print(f"[gate] Dedupe check warning: {e}")

          print(f"America/New_York now: {now.isoformat()} | run_ok={ok}")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as f:
              f.write(f"run_ok={'true' if ok else 'false'}\n")
          PY

      - name: Run canonical daily pipeline
        if: github.event_name == 'workflow_dispatch' || steps.gate.outputs.run_ok == 'true'
        env:
          PRICECALL_ROOT: ${{ github.workspace }}
          PRICECALL_STRICT_DRIVE_ONLY: "1"
          PRICECALL_ALPHA_FAMILY: "part2a21"
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: |
          python run_tuesday_prediction.py --force

      - name: Verify critical artifacts were created
        if: github.event_name == 'workflow_dispatch' || steps.gate.outputs.run_ok == 'true'
        run: |
          missing=0

          for path in \
            artifacts_part2_g532/predictions/part2_g532_summary.json \
            artifacts_part2b_xgb/predictions/part2b_xgb_summary.json \
            artifacts_part2c_bnn/predictions/part2c_bnn_summary.json \
            artifacts_part2a_alpha/predictions/part2a21_alpha_positions.csv \
            artifacts_part3/prediction_log.csv \
            artifacts_part3_v1/v1_final_production_tape.csv \
            artifacts_part6/part6_meta.json \
            artifacts_part7/current_target_weights.json \
            artifacts_part8/part8_meta.json \
            artifacts_part9/live_attribution_report.json
          do
            if [ ! -e "$path" ]; then
              echo "MISSING: $path"
              missing=1
            else
              echo "OK: $path"
            fi
          done

          if [ "$missing" -ne 0 ]; then
            echo "Critical production artifacts were not created."
            exit 1
          fi

      - name: Commit updated production artifacts
        if: success() && (github.event_name == 'workflow_dispatch' || steps.gate.outputs.run_ok == 'true')
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Part 9 is produced during the run for local downstream use,
          # but it is NOT committed from this workflow.
          # This avoids rebase conflicts with the workflow that also refreshes
          # live_attribution_report.json.
          git restore --source=HEAD --staged --worktree artifacts_part9/live_attribution_report.json 2>/dev/null || true

          for path in \
            artifacts_part2_g532/predictions \
            artifacts_part2b_xgb/predictions \
            artifacts_part2c_bnn/predictions \
            artifacts_part2a_alpha/predictions \
            artifacts_part3 \
            artifacts_part3_v1 \
            artifacts_part6 \
            artifacts_part7 \
            artifacts_part8 \
            artifacts_part10_bot
          do
            if [ -e "$path" ]; then
              git add -f "$path"
            fi
          done

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Update daily production artifacts [skip ci]"
          git pull --rebase --autostash origin "${GITHUB_REF_NAME}"
          git push origin HEAD:"${GITHUB_REF_NAME}"
